<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ODataExample.Views.MenuPage">

    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0, 20, 0, 0" />
    </ContentPage.Padding>
    <ContentPage.Content>
        <StackLayout BackgroundColor="Silver">
            <Button Margin="0,40,0,0" Text="Addresse prüfen" TextColor="White" BackgroundColor="Navy" Command="{Binding GoNewAddressCommand}" />
            <Button Text="Addressverwaltung" TextColor="White" BackgroundColor="Navy" Command="{Binding GoAddressesCommand}" />
            <Button Text="Einstellungen" TextColor="White" BackgroundColor="Navy" Command="{Binding GoSettingsCommand}" />
			<Button Text="About" TextColor="White" BackgroundColor="Navy" Command="{Binding GoAboutCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>